import React, { FC } from 'react'
import { Spinner } from '.'

export default {
  component: Spinner,
  title: 'components/Spinner',
}

export const SpinnerComponent: FC = () => {
  return <Spinner />
}
